# =============================================================================
# Machine Learning-Based Network Intrusion Detection and Anomaly Classification
# Python dependency specification
#
# Target Python: 3.11 - 3.13  (developed and executed on CPython 3.13.9)
# Install with:  pip install -r requirements.txt
#
# Version pins use compatible-range operators (>=, <) rather than exact pins so
# the project installs cleanly across minor interpreter versions. The exact
# resolved versions used to produce the committed results are recorded in
# `reports/environment_versions.txt` (generated by `python -m src.report`).
# =============================================================================

# --- Core scientific stack ---------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0

# --- Machine learning --------------------------------------------------------
scikit-learn>=1.4,<2.0
xgboost>=2.0,<4.0
lightgbm>=4.1,<5.0            # optional 4th model (gradient boosting comparison)
imbalanced-learn>=0.12,<1.0   # SMOTE ablation only; NOT used in the final model

# --- Explainability ----------------------------------------------------------
shap>=0.44,<1.0

# --- Visualisation -----------------------------------------------------------
matplotlib>=3.8,<4.0

# --- Model persistence -------------------------------------------------------
joblib>=1.3,<2.0

# --- Deployment prototype ----------------------------------------------------
streamlit>=1.30,<2.0

# --- Notebook execution ------------------------------------------------------
jupyter>=1.0,<2.0
jupyterlab>=4.0,<5.0
ipykernel>=6.29,<7.0
nbformat>=5.9,<6.0
nbconvert>=7.10,<8.0

# --- Submission bundle -------------------------------------------------------
# Used only by `python -m src.deliverables`, which renders the reports and decks
# into the .docx / .pptx formats the assignment accepts.
python-docx>=1.1,<2.0
python-pptx>=0.6.23,<2.0

# --- Testing -----------------------------------------------------------------
pytest>=8.0,<9.0

# --- Data acquisition helpers ------------------------------------------------
requests>=2.31,<3.0
